import template from './template.js'
import styles from './styles.js'

const appOther = () => {

    const state = {
        title: 'Other Page!!!'
    }

    return {
        state,
        template,
        styles
    }
}

export default appOther