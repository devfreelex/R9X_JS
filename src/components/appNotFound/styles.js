export default ({ ctx, css }) => css`
    ${ctx} .notfound-wrapper {
        display:block;
        float:left;
        width:100%;
        padding:45px;
        text-align: center;
        color:red
    }
`