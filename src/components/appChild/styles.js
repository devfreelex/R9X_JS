export default ({ ctx, css }) => {
    return css`
        ${ctx} .child-wrapper {
            display:block;
            float:left;
            width:100%;
            padding:30px;
            background:pink;
        }    
`
}